
type Employee {
  id: Int
  name: String
  email: String
  dob: String
  doj: String
  created_at: String
  updated_at: String
  tasks: [Task]
}

type Department {
  dept_id: Int
  dept_name: String
  emps: [Employee]
}

type Query {
  employee(id: Int!): Employee
  employees: [Employee]
}
